#this is a config file for the market assets
defaults:
    equity:
      stock:
        default: &equity_index_default
          extractor: "default"
          points: ["spot"]

market_coordinates:
    equity:
      index:
        snp:
          <<: *equity_index_default


